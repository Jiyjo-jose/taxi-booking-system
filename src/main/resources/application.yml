spring:

  datasource:

    url: jdbc:postgresql://localhost:5432/taxii
    username: postgres
    password: Aish@123
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true
logging:
  level:
    org.springframework: INFO

server:
  port: 8088
